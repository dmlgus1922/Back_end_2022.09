<!DOCTYPE html>
        <html lang="en">
        <head>
            <%- include(`${path}/head`) %>
        </head>
        <body>
            <script>
                let answer = confirm('Are you sure to delete?');
                if (answer)
                    location.href = '/deleteConfirm/<%= id %>';
                else
                    location.href = '/';
            </script>
        </body>
        </html>